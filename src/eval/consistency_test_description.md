consistency tests verifies whether the solver output (the test's input) is consistent with the canon lore.

that is, if we replay a season using the its "canon" rule(season 1-2: sum of judge and audience rank; season 3-27: fan percentage and score percentage taken average (currently 50-50); season 28+ added the judge's last two modification, see more at `first_stage_modeling.md`) 

we expect a PERFECT MATCH of the (the same people are eliminated for every week). measure discrepancy as to report any jaccard similarity smaller than one.