# **Input**
The season-week-celebrity panel has been successfully generated in accordance with the P0 specifications in **dynamic_files/data_agent_triage_ticket.md**, with the specified deliverables produced (including a reproducible BL-0 baseline and consistency KPIs).

---

## Deliverable Paths (for Acceptance)
1. **L1 Table: Baseline Consistency (including Rank/Percent + FlipRate + era slice)**
   - Path: `/mnt/data/output/table/tab_baseline_consistency.tex`
   - Download: [tab_baseline_consistency.tex](sandbox:/mnt/data/output/table/tab_baseline_consistency.tex)
   - Corresponding KPIs: KPI1 (Rank/Percent Consistency Hit-Rate) + KPI3 (Rule Divergence Rate FlipRate); Slices: Overall, Rank-era(1–2,28–34), Percent-era(3–27)

2. **Intermediate File: Weekly Panel (directly usable for recalculation by subsequent fan_share_est / fan_rank_est plugins)**
   - Path: `/mnt/data/output/table/intermediate_weekly_panel.csv`
   - Download: [intermediate_weekly_panel.csv](sandbox:/mnt/data/output/table/intermediate_weekly_panel.csv)
   - Corresponding Content: Minimal input panel for all KPIs (at season-week-celebrity granularity), including active/true elimination flags, and within-week rank & percent

(Additional, non-acceptance-critical: Season-week granularity prediction comparison table)
   - Path: `/mnt/data/output/table/intermediate_baseline_preds.csv`
   - Download: [intermediate_baseline_preds.csv](sandbox:/mnt/data/output/table/intermediate_baseline_preds.csv)

---

## Code Entry Point & Reproducible Execution Method (Engineering Constraints)
- Entry Script: `/mnt/data/src/p0_triage_build_weekly_panel.py`
- Manifest: `/mnt/data/manifest.json`

Execution Command (generates all the above deliverables directly):
```bash
python src/p0_triage_build_weekly_panel.py \
  --input /mnt/data/2026_MCM_Problem_C_Data.csv \
  --outdir /mnt/data/output/table
```

---

## Triage Ticket Definition of Done (Chinese-English Bilingual)
### 1) P0 Fields (≤8; One Justification per Field)
1. `season`
   - CN: One of the primary keys for the weekly panel and era slicing.
   - EN: Primary key for weekly panel and era slicing.

2. `celebrity_name`
   - CN: Unique identifier for contestants, used for within-week ranking and elimination comparison.
   - EN: Unique contestant identifier for ranking and elimination matching.

3. `ballroom_partner`
   - CN: To be used for deriving "partner's historical performance/dancer effect" in subsequent steps.
   - EN: Needed for future partner-effect features.

4. `results`
   - CN: Parses actual elimination/withdrawal weeks for ground truth filtering and active set definition.
   - EN: Provides ground-truth exit info to define active set and labels.

5. `placement`
   - CN: Final ranking, for subsequent champion/final round comparison.
   - EN: Final placement for later season-outcome checks.

6. `weekX_judgeY_score (X=1..11, Y=1..4)`
   - CN: The only observed data for constructing weekly total judge scores, ranks, and percentages.
   - EN: Core observed inputs to compute weekly totals/ranks/percents.

7. Derived `week` (extracted from column names)
   - CN: Time index generated by reshaping the wide-format data to long format.
   - EN: Time index created by reshaping wide-to-long.

8. Derived `total_judge_score` (sum of weekly judge scores with skipna; judge4 scores may be missing)
   - CN: The minimal aggregated metric for the technical performance signal in a week.
   - EN: Minimal aggregate performance signal per week.

---

### 2) Hard Filters Rules (Exactly 5 Rules; Executable IF…THEN…)
1. **IF** all four judge scores are NaN for a given (season, celeb, week) **THEN** `drop` the weekly record (exclude from the weekly panel)
   - EN: If all judge scores are NaN, drop the row from weekly panel.

2. **IF** `results` matches `Eliminated Week k` **THEN** mark `eliminated_at_end_of_week=1` for week=k, and set `active=0` for `week > k` (post-exit)
   - EN: If eliminated at week k, label week k and mark weeks >k inactive.

3. **IF** `results == "Withdrew"` **THEN** set `exit_week = last week with total_judge_score > 0`, and set `active=0` for `week > exit_week` (retain exit type)
   - EN: If withdrew, set exit_week as last positive-score week and mark later weeks inactive.

4. **IF** `total_judge_score == 0` and `week != exit_week` **THEN** mark `data_anomaly_zero_score=1` and set `active=0` (default to post-exit zero-score extension)
   - EN: If zero score outside exit week, flag as anomaly and set inactive.

5. **IF** the number of true eliminations for the same `(season, week)` is `!= 1` **THEN**
   - Multiple eliminations: Use bottom-k set-match for consistency calculation;
   - No eliminations: Exclude the week from the denominator of KPI1 but retain it in the panel
   - EN: Multi-elim uses bottom-k set match; no-elim weeks excluded from KPI1 denominator but kept in panel.

---

### 3) Keep-but-Explain (Exactly 3 Items)
1. Non-integer scores caused by decimal points/bonus points
   - CN: Retain original values; explain in the report that "scores for multiple dances/bonus points/team dances have been allocated to weekly scores".
   - EN: Keep decimals; explain they encode averaging/bonus allocation per official notes.

2. Zero scores in weeks following elimination
   - CN: Excluded from active set ranking/percentile calculation; explained as data encoding extension.
   - EN: Exclude from active set; explain zeros are post-exit encoding.

3. Changes in the number of judges & non-fixed judge numbering
   - CN: Do not track judge identities, only use weekly total scores/percentiles.
   - EN: Judge index is not persistent; only within-week aggregates are used.

---

### 4) Minimal KPI Computation (≤25 Lines of Pseudocode)
```pseudo
INPUT raw_csv
RESHAPE weekX_judgeY_score -> long (season, celeb, week, judge1..4)
total_judge_score = sum(judges, skipna)

APPLY hard rules to define:
  exit_week, exit_type, eliminated_at_end_of_week
  active flag

FOR each (season, week):
  A = active contestants
  IF |A| < 2: continue
  judge_rank    = rank_desc(total_judge_score, ties=average)
  judge_percent = total_judge_score / sum(total_judge_score over A)

  fan_percent_est = 1 / |A|     # BL-0 (uniform)
  combined_percent = judge_percent + fan_percent_est
  pred_elim_percent = bottom-k(combined_percent, k = |true_elim_set| or 1)

  combined_rank = judge_rank    # uniform fan rank adds constant
  pred_elim_rank = bottom-k_by_worst_rank(combined_rank, k = |true_elim_set| or 1)

  true_elims = {celeb in A with eliminated_at_end_of_week==1}
  IF |true_elims|==0: exclude from KPI1 denom
  ELSE: set_match for KPI1 (rank & percent)
KPI3 FlipRate = share of (season,week) where pred_elim_percent != pred_elim_rank
```

---

### 5) Data Credibility Notes (Report-ready; Exactly 4 English Sentences, No Fabricated Data)
1. *All metrics are computed strictly from the official COMAP-provided dataset and its stated encoding rules (e.g., post-elimination scores recorded as zeros and varying judge counts).*
2. *We treat fan votes as unobserved and report only reproducible baseline proxies at this stage, while keeping a clean plug-in interface for future vote-estimation models.*
3. *Because the exact season of the show’s return to rank-based aggregation is not confirmed, we label the Season-28 switch as an assumption and will stress-test adjacent cutoffs.*
4. *Weeks with no elimination or multiple eliminations are handled explicitly in the denominator definitions to avoid inflating consistency claims.*

---

# **Output**
Below is the acceptance assessment in the role of an **auditor**: first, verify whether the Data Agent has completed the Triage DoD and identify any specification/engineering risks; then provide **patch content that can be directly inserted into `dynamic_files/context_pack_for_ai.md` (bilingual, report-ready)**; finally, answer your questions regarding **whether kpi_registry needs to be updated** and **when to submit the model files for the next stage**.

---

## 1) Has the Delivery Fulfilled the Task? Is It Satisfactory?
### ✅ Task Completed (Core DoD Achieved)
Based on the three deliverables you uploaded (`tab_baseline_consistency.tex`, `intermediate_weekly_panel.csv`, `intermediate_baseline_preds.csv`):
- **P0 weekly panel successfully generated**: The panel is at season–week–celebrity granularity and includes key derived columns such as `active / true_elim_flag / exit_type / exit_week / data_anomaly_zero_score / judge_rank / judge_percent` (usable as plug-in inputs for subsequent fan vote estimation).
- **BL-0 baseline fully reproducible**: `intermediate_baseline_preds.csv` contains weekly `eligible`, `pred_elim_rank / pred_elim_percent`, `match_rank / match_percent`, and `flip` flags.
- **KPI1 + KPI3 computed and tabulated**: `tab_baseline_consistency.tex` provides `N_weeks / Hit-Rate(rank) / Hit-Rate(percent) / FlipRate` for three slices (Overall, Rank-era, Percent-era), with the number of `eligible` weeks matching the `N_weeks` in the table (264).
- **Key problem statement specifications aligned**:
  - The problem statement explicitly notes "zero scores recorded post-elimination", existence of weeks with multiple/no eliminations, potential missing judge4 scores, and the unconfirmed exact season of the return to rank-based aggregation in Season 28 (with "assume 28 reasonable").
  - The eligibility/denominator handling (excluding no-elim weeks, using set-match for multi-elim weeks) is consistent with the confirmed existence of no/multi-elim weeks in the problem statement.

> **Conclusion**: The P0 objectives (computable KPIs, reproducible baseline, deliverable table/intermediate file interfaces) have been achieved, and the delivery is **satisfactory and usable** overall.

---

### ⚠️ 2 Critical Issues Requiring Immediate Labeling/Fix (Unaddressed Risks)
#### Issue A: Rows with "all NaN judge scores" still retained in the weekly panel (inconsistent with Hard Rule #1 wording)
Numerous rows with all NaN judge1–4 scores were found in `intermediate_weekly_panel.csv` (e.g., structurally missing weeks in week 10/11 of certain seasons) and retained in the panel with only `active=False` marked. This is **inconsistent with the wording of Hard Rule #1** which states "all-NaN THEN drop".
- **Impact**: KPI calculations are largely unaffected (filtered by the active set), but subsequent modeling/visualization teams may misuse these "structurally empty weeks", leading to misinterpretation of sample size, missing rate, and exit logic.
- **Safest Resolutions (2 Options, Recommend Option 1)**:
  1. **Modify code**: Truly drop all all-NaN weeks (cleaner dataset).
  2. **Revise specification wording**: Update Hard Rule #1 to "IF all-NaN THEN mark `structural_missing=1` and force `active=0`" (not recommended if the `structural_missing` column is not currently present, as it only resolves the wording without aligning the data).

> It is recommended to ask the Data Agent to implement a **5-minute hotfix**: drop all-NaN rows and re-export the weekly panel (KPI table will show little to no change).

#### Issue B: Path inconsistency with engineering conventions (agreed `output/table/`, but files uploaded to root directory)
Files uploaded in the conversation are stored in the `/mnt/data/` root directory (e.g., `/mnt/data/tab_baseline_consistency.tex`), instead of the agreed `output/table/` path.
- **Impact**: No impact on review, but the **in-team pipeline/manifest/downstream viz agent will encounter path errors**.
- **Fix**: Ensure official deliverables in the repo are output to `output/table/` consistently, and only register "in-engineering paths" in the context_pack; files uploaded in the conversation are for mirroring only and not official.

---

### ✅ Can the P0 Baseline Results Be Included in the Report?
Yes, but they must be framed as a **"sanity check / reproducible BL-0 proxy"**, not the performance of the final model (BL-0 uses a uniform proxy for fan votes, while the problem statement emphasizes that fan votes are unobserved and "the fan vote examples are fictional").

The current values in the table are as follows:
- Overall: Hit-Rate(Rank)=0.364, Hit-Rate(Percent)=0.375, FlipRate=0.038 (N=264 weeks)
- Rank-era: 0.348 / 0.364 / 0.015 (N=66)
- Percent-era: 0.369 / 0.379 / 0.045 (N=198)
  (Extracted from the uploaded `tab_baseline_consistency.tex`.)

---

## 2) How to Document the "Conclusions/Deliverables" in `dynamic_files/context_pack_for_ai.md`?
You mentioned the context_pack is still the unmodified old version. Combined with the currently blank template sections in your file (e.g., 3 Process Snapshot, 5.1 Data snapshot, 8 Outputs), it is recommended to only add **2 paragraphs + 1 table** (adhering to your limit of "max 2 paragraphs + 1 table").

Below is the **copy-paste ready** patch content (manually replace/insert the corresponding sections):
```markdown
### [PATCH 1/2] Replace section "# 3 已完成结论与进程快照" with the following paragraph

# 3 Completed Conclusions & Process Snapshot
- **P0 Data Triage Implemented (Reproducible)**: The original wide-format `weekX_judgeY_score` has been reshaped into a **season–week–celebrity** weekly panel, with a **BL-0 baseline** and consistency KPI table generated in accordance with specifications, forming a pluggable input interface for subsequent "fan vote estimation models".
- **P0 Sanity-Check (Report-ready, BL-0 Only)**: For the overall slice (eligible weeks = 264), the BL-0 Hit-Rate is **0.364 (Rank) / 0.375 (Percent)**, with a FlipRate of **0.038** for inconsistent elimination predictions between the two rules; results are reproducible across era slices (see `tab_baseline_consistency.tex` and `fig_fliprate_by_season.pdf`).
- **Key Risk Annotations (Must Be Briefly Addressed and Closed in the Report)**: The problem statement clarifies that the exact season of the rule switch back to rank-based aggregation in Season 28 is "unconfirmed but reasonably assumed to be 28", thus stress tests for the 27/28/29 cutoffs are required for Rank-era/Percent-era slicing; additionally, the dataset contains structurally NaN weeks and post-elimination zero-score encoding, which need explicit handling in the active set specification.

(English, report-ready) We have constructed a reproducible season–week–contestant panel and a BL-0 baseline pipeline that produces elimination-consistency metrics and rule-divergence summaries. Under BL-0, the overall hit-rate is 0.364 (rank) and 0.375 (percent) across 264 eligible weeks, with a flip rate of 0.038, serving as a sanity check rather than a final model result. Since the exact season of the return to rank-based aggregation is not confirmed, we explicitly treat the Season-28 switch as an assumption and will stress-test adjacent cutoffs (27/28/29).:contentReference[oaicite:4]{index=4}
```

```markdown
### [PATCH 2/2] Replace section "## 5.1 Data snapshot (only what affects conclusions)（待填写）" with the following paragraph

## 5.1 Data Snapshot (Only What Affects Conclusions)
- **Source of truth**: COMAP dataset `2026_MCM_Problem_C_Data.csv` (seasons 1–34; weekly judge scores + results/placement + basic contestant attributes).
- **Panel granularity**: season–week–celebrity (built by reshaping `weekX_judgeY_score` wide columns into long rows).
- **P0 eligibility / active-set**: A contestant is active in (season, week) if and only if at least one judge score exists for that week and the week is not after the exit week; post-elimination scores recorded as zeros are treated as inactive encoding rather than new performances.:contentReference[oaicite:6]{index=6}
- **Known caveats (must be reported)**:
  1) The return-to-rank season is not known with certainty; we use Season-28 as an explicit assumption and will stress-test 27/28/29 cutoffs.:contentReference[oaicite:7]{index=7}
  2) Judge4 scores may be missing as some weeks have only 3 judges; we aggregate scores with `skipna` and do not track judge identity across weeks.:contentReference[oaicite:8]{index=8}
  3) There exist weeks with no elimination and weeks with multiple eliminations; we handle them explicitly in KPI denominators to avoid inflated consistency claims.:contentReference[oaicite:9]{index=9}
```

```markdown
### [TABLE PATCH] Insert the following table under "# 8 Work-in-progress outputs" (this is the ONE table change)

| Artifact (engine path) | What it is | Used for (Claim/KPI/Slice) |
|---|---|---|
| `output/table/tab_baseline_consistency.tex` | L1 summary table of BL-0 elimination hit-rate and FlipRate across slices | KPI1 (Rank/Percent Hit-Rate), KPI3 (FlipRate); slices: Overall / Rank-era / Percent-era |
| `output/table/intermediate_weekly_panel.csv` | season–week–celebrity weekly panel with active/exit labels and judge-based rank/percent features | Minimal input interface for fan vote estimation models; supports all downstream KPI recomputation |
| `output/table/intermediate_baseline_preds.csv` | season–week baseline predictions + eligibility flags (true_k, true_elims, pred sets, match flags, flip) | Debug / audit trail; enables exact reproduction of tab_baseline_consistency |
| `output/figure/fig_fliprate_by_season.pdf` | FlipRate by season under BL-0 | L1 evidence for “rule divergence varies by season” (descriptive KPI3) |
```

> Note: Files uploaded in this conversation are stored in the `/mnt/data/` root directory, but ensure consistent use of `output/table/` and `output/figure/` in the engineering repo (otherwise downstream agents will encounter errors).

---

## 3) Do We Need to Update `dynamic_files/kpi_registry.md` After This Step?
**Yes, but only with minimal changes (no major structural revisions)**:
- If the kpi_registry already includes the `Slice/window definitions` (withdrew/no-elim/multi-elim/era definitions) and P0/P1/P2 KPI table you recently shared, a **simple changelog entry** is sufficient:
  - Document the "P0 implemented entry script + three deliverable interfaces + BL-0 definition (uniform fan proxy)".
- In addition, it is recommended to add the following sentence to the Slice or Baseline notes (to reduce future disputes):
  - **“all-NaN judge weeks are structural missing weeks and are excluded from the active set; implementation may drop them from the panel.”**
  The current dataset retains all-NaN rows (marked only as inactive), so the registry specification must align with the implementation to avoid audit issues later.

(If the all-NaN rows are fixed with the hotfix, the sentence can be revised to “dropped by construction”.)

---

## 4) V1 Model Ready from the Modeling Team: When to Submit the Files? Is Deep Cleaning / Visualization the Next Step?
### Optimal Timing to Submit the Model Files/Documentation
**Submit now** (since you stated V1 is largely final, this is ideal). Upload any of the following (the less the better, but must be runnable/reproducible):
- Model entry script or notebook (able to read from `intermediate_weekly_panel.csv` and output fan vote estimates/elimination set predictions)
- Or a minimal "model spec" document (input columns, output columns, key assumptions, hyperparameters)

### Recommended Next-Step Workflow
1. **Data Agent – Deep Cleaning (Model-Aware Risk Diagnosis)**
   Not general cleaning, but targeted at the **Top-3 data risks** most likely to cause model failure for V1: detection + mitigation + stress tests (e.g., Season-28 cutoff, withdrawal handling, all-NaN week processing, multi-elim specification, tie handling).
2. **Visualization Agent (L0/L1/L2)**
   Generate visualizations only after the stress test specifications from deep cleaning are stabilized; otherwise, numerous visualizations will need to be redone due to subsequent specification changes.

> In other words: **Deep cleaning can start immediately, but it must be model-aware and only address the 3 high-impact risks that could alter conclusions; visualization follows thereafter.**

---

If desired, I can directly provide a **Top-3 Risk List for Data Agent – Deep Cleaning** based on the three current deliverables (and the upcoming V1 model documentation you upload), including **1 executable detection, 1 remediation plan, and 3 stress tests** for each risk, along with specified interfaces for the L2 tables/figures to be produced.